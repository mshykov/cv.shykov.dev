# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# AI coding assistant local artifacts (per-developer, never committed).
# .claude/ is intentionally NOT ignored — its skills/ are project config.
# AGENTS.md / CLAUDE.md are intentionally NOT ignored — they are project config.
.claude/launch.json
.antigravitycli/
.antigravity/
.codex/
.openai/
.openAI/
.aider*
.continue/
.windsurf/
.windsurfrules
.gemini/
.copilot/
.github/copilot-instructions.md
.cline/
.clinerules
.roo/
.qodo/
.tabnine/
.sourcegraph/
.cody/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# Personal / local-only scratch — never committed.
# Real test CVs (personal data) for parser debugging, secrets, session notes.
_do_not_commit/
